# MongoDB Connection String (replace with your actual URI)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.ljlp3lc.mongodb.net/<database>?retryWrites=true&w=majority

# Server Port
PORT=5000

# JWT Secret Key (generate a strong random string)
JWT_SECRET=generate_a_strong_random_secret_key_here

# Admin Credentials (change these!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=StrongPassword@2024